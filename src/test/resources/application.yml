spring:
#  datasource:
#    url: jdbc:h2:mem:test
#  인메모리 DB를 사용하도록 바꾸셨습니다.
#  따로 설정해주지 않으면 application에서 실행하면서 인메모리 DB를 만들어 실행할 때 임의로 ID를 부여해주기 때문에 저렇게 보이는 것입니다.
#    username: sa
#    password:
#      driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create
#      properties:
#        hibernate:
#          show_sql: true
#          format_sql: true
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html

logging:
  level:
    org.hibernate.SQL: debug
#    org.hibernate.orm.jdbc.bind: trace